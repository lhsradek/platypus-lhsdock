
volumes:
  apm:
    external: true
    name: "${APP_ID}-apm"
  esdata01:
    external: true
    name: "${APP_ID}-es01"
  esdata02:
    external: true
    name: "${APP_ID}-es02"
  esdata03:
    external: true
    name: "${APP_ID}-es03"
  epsdata:
    external: true
    name: "${APP_ID}-eps"
  fleet:
    external: true
    name: "${APP_ID}-fleet"
  filebeat:
    external: true
    name: "${APP_ID}-filebeat"
  heartbeat:
    external: true
    name: "${APP_ID}-heartbeat"
  kibana:
    external: true
    name: "${APP_ID}-kibana"
  logstash:
    external: true
    name: "${APP_ID}-logstash"
  metricbeat:
    external: true
    name: "${APP_ID}-metricbeat"
#    
# You can remove this in docker-setup.yml. It's for my prune volume deletion protection:
#
  wordpressdb99:
    external: true
    name: wordpress-db-${WP_NUM}
  wordpresspma99:
    external: true
    name: wordpress-pma-${WP_NUM}
  wordpressredis99:
    external: true
    name: wordpress-redis-${WP_NUM}
  wordpressfleet99:
    external: true
    name: wordpress-fleet-${WP_NUM}
  wordpresswp99:
    external: true
    name: wordpress-wp-${WP_NUM}
  tomboladb:
    external: true
    name: tombola-db
  tombolafleet:
    external: true
    name: tombola-fleet
